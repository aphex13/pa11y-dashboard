version: '3'

services:
  mongodb:
    image: mongo:6.0
    volumes:
      - mongodb_data:/data/db
    ports:
      - "27017:27017"
    networks:
      - pa11y-network
    restart: always
    healthcheck:
      test: ["CMD", "mongo", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  pa11y-dashboard:
    build: .
    ports:
      - "4100:4100"
    depends_on:
      mongodb:
        condition: service_healthy
    environment:
      - NODE_ENV=production
      - PORT=4100
    networks:
      - dokploy-network
    restart: always

networks:
  pa11y-network:
    driver: bridge

volumes:
  mongodb_data:
